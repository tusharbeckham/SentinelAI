# Runtime dependencies are deliberately minimal.
#
# There is no scikit-learn, no XGBoost, no FastAPI here. Isolation Forest,
# gradient-boosted trees, Shapley attribution, logistic stacking and the
# authenticated HTTP API are all implemented directly against NumPy/pandas and
# the standard library. Fewer dependencies means a smaller CVE surface and no
# hidden version-coupled behaviour in the results.
numpy>=2.5.1,<3.0
pandas>=2.2,<4.0
